# DeepSDF Planes 600-Epoch Results

This folder documents the original airplane DeepSDF reproduction already present
in the parent repository.

Small visualization images are copied into:

```text
figures/deepsdf_planes_600epoch/
```

Large or binary artifacts are intentionally not duplicated here:

- `models/ModelParameters/600.pth`
- `models/LatentCodes/600.pth`
- `generated/GeneratedPlanes/*.ply`

Those artifacts are already present in the parent repository and can also be
mirrored in the external Google Drive artifact folder if a fully self-contained
release bundle is needed.


